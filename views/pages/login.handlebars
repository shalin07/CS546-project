	<div>
		<a href="/aboutus">About Us</a>
	</div>
	{{#if success_messages }}
				<div class="alert alert-success">{{success_messages}}</div>
				{{/if}}
				{{#if error_messages }}
				<div class="alert alert-danger">{{error_messages}}</div>
				{{/if}}
	<h1>f<span>oo</span>d c<span>o</span>urt l<span>o</span>g<span>in</span> f<span>o</span>rm</h1>
	<div class="wthree-form">
			<h2>Fill out the form below to login</h2>
		<div class="w3l-login form">
			<form action="/users/login" method="post">
				<div class="form-sub-w3">
					<input type="text" name="email" placeholder="Email" required=""/>
				</div>
				<div class="form-sub-w3">
					<input type="password" name="password" placeholder="Password" required=""/>
				</div>  	
				<div class="submit-agileits">
					<input type="submit" value="Login">
				</div>
					<a href="/users/registration">Don't have an Account yet? Register</a>
			 </form>
		</div>
	</div>